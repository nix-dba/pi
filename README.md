# Pi Agent

Sandboxed pi agent environment with code intelligence, running via Nix with Bubblewrap isolation.

## Usage

In your repository root run:

```sh
nix run github:nix-dba/pi --refresh --accept-flake-config
```
